spring:
  datasource:
    url: jdbc:mysql://localhost:3306/youbank
    username: root
    password: 1234

  jpa:
    database: mysql
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        jdbc:
          batch_size: 20
        format_sql: true
        order_updates: true
        show_sql: true

---

